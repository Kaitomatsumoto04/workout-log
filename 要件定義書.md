# 要件定義書 — workout-log（筋トレ記録ウェブアプリ）

作成日: 2026-08-24
作成方法: 既存コード（index.html / script.js / style.css）と HANDOVER.md からのリバースエンジニアリング
※この文書は「先に要件を決めてから作った」ものではなく、現状の実装を読み解いて後付けで文書化したものです。今後の仕様変更時は、この文書と実装の両方を更新してください。

---

## 1. 概要・目的

自分の筋トレ内容（部位・種目・重量×回数）を記録し、後から振り返られるようにする個人用ウェブアプリ。
最大の目的はアプリを作ることそのものではなく、**フレームワークに頼らずJS（DOM操作・イベント・状態管理）の基礎を手を動かして習得すること**。そのため実装は意図的に「まず動くもの→少しずつ機能追加」の順で進められている。

## 2. 想定ユーザー・利用環境

- 利用者: 開発者本人のみ（1人称利用、ログイン機能なし）
- 利用端末: スマートフォン想定のレイアウト（`max-width: 480px`、`viewport`設定あり）
- 利用ブラウザ: モダンブラウザ（`backdrop-filter`, `Chart.js` 等を使用）
- データの保存範囲: **端末・ブラウザ単位**（localStorageのため、他端末とは同期しない）

## 3. スコープ

### 3.1 対象範囲（実装済み）
- 記録の入力・保存・一覧表示・削除
- 部位／種目のマスタ管理（種目の追加）
- 月カレンダーによる記録日の可視化
- 期間・部位・種目を指定した重量推移グラフ

### 3.2 対象外（意図的に見送り／却下）
- サーバー・DBを用いたバックエンド構成（学習コストが高いため第1〜2弾では不採用）
- ユーザーアカウント・ログイン認証
- 他人の記録の閲覧（フレンド機能）
- Reactなど何らかのフロントエンドフレームワーク（土台となる素のJS習得を優先）
- リポジトリのPrivate化（無料プランではPrivateだとGitHub Pagesで公開できないため）

## 4. 画面構成（3画面・1ファイル内でJS表示切替）

`index.html` 内に3つの `<section class="screen">` を用意し、`showScreen(screenId)` で `.active` クラスを付け替えることで1ページ内の画面遷移を実現する（[script.js:1-32](script.js#L1-L32)）。

| 画面ID | 名称 | 役割 |
|---|---|---|
| `screen-home` | ホーム | 月カレンダー＋直近履歴の一覧、他2画面への導線 |
| `screen-record` | 記録する | 新規記録の入力・保存 |
| `screen-review` | 振り返る | 期間・部位・種目を指定した重量推移グラフ表示 |

## 5. 機能要件

### 5.1 ホーム画面（`screen-home`）

- **月カレンダー**
  - 表示中の年月を見出しに表示（例: `2026年7月`）
  - 「‹」「›」ボタンで前月・翌月に移動（年をまたぐ場合は自動繰り上げ／繰り下げ）（[script.js:491-507](script.js#L491-L507)）
  - 記録が存在する日は円形マスを塗りつぶして強調表示（`.has-record`）
  - 日付マスをタップすると、その日の記録一覧をカレンダー下部（`#day-detail`）にテキスト表示。記録がなければ「記録なし」と表示（[script.js:457-487](script.js#L457-L487)）
  - 選択中の日付マスには枠線を表示（`.selected`）
  - 選択した日付は `selectedDate`（`"YYYY-MM-DD"`）として保持し、記録する画面の日付欄に引き継ぐ
  - カレンダーを描き直すと選択枠が消えるため、`renderCalendar()` の実行時に `selectedDate` も未選択に戻す（表示と状態を一致させるため）
- **画面遷移ボタン**: 「記録する」「振り返る」
  - 配置は**カレンダー直下・直近の履歴の上**（主要な操作を画面上部に置き、履歴をスクロールしなくても押せるようにするため）
  - 「記録する」押下時、カレンダーで日付を選択済みならその日付を記録画面の日付欄に自動入力してから遷移する（[script.js:14-20](script.js#L14-L20)）。未選択の場合は日付欄を触らない
- **直近の履歴一覧**
  - 保存済み記録を**日付ごとにグループ化**して新しい日付順に表示（[script.js:156-217](script.js#L156-L217)）
  - 各記録行に「部位 種目 重量kg×回数, ...」の形式でセット内容を表示
  - 各記録行に削除ボタンを表示。押下時は確認ダイアログ（`confirm`）を出してから削除（[script.js:220-231](script.js#L220-L231)）
  - 記録が0件の場合は「まだ記録がありません」と表示

### 5.2 記録する画面（`screen-record`）

入力項目:
- 日付（`<input type="date">`、初期値なし。ホームのカレンダーで日付を選んでから遷移した場合のみ自動入力される）
- 部位（プルダウン固定5種: 胸／背中／腹筋／腕／下半身）
- 種目（部位選択に連動して選択肢が動的に変わるプルダウン）（[script.js:55-78](script.js#L55-L78)）
- セット（重量kg・回数の組を複数行、可変）

機能:
- **部位→種目連動**: 部位を選ぶと、その部位に登録済みの種目一覧に応じて種目プルダウンを再構築する。未選択時は「先に部位を選択」を表示
- **種目の追加**: 「＋ 種目を追加」ボタン→`prompt`でその場で種目名を入力→選択中の部位の種目マスタに追加し、即座にプルダウンへ反映・選択状態にする（[script.js:82-106](script.js#L82-L106)）。部位未選択時はアラートで警告。空文字・キャンセル時は追加しない
- **セット行の追加**: 「＋ セットを追加」で空のセット入力行を1行追加（[script.js:123-126](script.js#L123-L126)）。セット1行分のDOM生成は `createSetRow()` に共通化され、追加・コピー・リセットの3か所から使い回される（[script.js:112-121](script.js#L112-L121)）
- **セットのコピー**: 「⧉ セットをコピー」で最後の1行の重量・回数の値を複製した新規行を追加。最後の行が未入力の場合はアラートで警告（[script.js:514-534](script.js#L514-L534)）
- **保存時のバリデーション**:
  - 日付・部位・種目のいずれかが未入力ならアラートを出して保存しない
  - 重量・回数が両方入力されているセット行のみ有効なセットとして採用する
  - 有効なセットが1件もない場合はアラートを出して保存しない
- **保存処理**: `records`配列に追加→localStorageに保存→履歴一覧とカレンダーを再描画→**入力フォームをリセット**→完了アラート（[script.js:233-277](script.js#L233-L277)）
  - 保存後も**記録画面に留まる**（同じ日に複数種目を続けて記録できるようにするため）。ホームへは「ホームに戻る」ボタンで明示的に戻る
- **保存後の入力フォームのリセット**（`resetRecordForm()`、[script.js:130-141](script.js#L130-L141)）: 同じ日に続けて別の種目を記録する際に前の種目の入力が残らないようにする
  - セット欄: 全行を破棄し、空の1行だけに戻す
  - 部位: 未選択（「選択してください」）に戻す
  - 種目: 部位が未選択になるため「先に部位を選択」に戻る
  - 日付: **あえて保持する**（同じ日に複数種目を続けて記録するケースが多いため）
- 「ホームに戻る」ボタンで保存せずにホームへ戻れる

### 5.3 振り返る画面（`screen-review`）

入力項目:
- 開始日・終了日（`<input type="date">`、任意入力）
- 部位・種目（記録画面と同様の連動プルダウン）

機能:
- 「グラフを表示」ボタン押下時、下記条件で `records` を絞り込む（[script.js:307-357](script.js#L307-L357)）:
  - 部位・種目が完全一致
  - 開始日が指定されていれば、記録日がそれ以降
  - 終了日が指定されていれば、記録日がそれ以前
  - 部位・種目未選択ならメッセージ表示のみで抽出しない
- 該当データが0件の場合は「該当する記録がありません」を表示し、既存グラフがあれば破棄する
- 該当データがある場合、日付の古い順に並べ替え、**日ごとにその日の最大重量**を算出して折れ線グラフ（Chart.js）を描画
  - Y軸は0始まり、軸タイトルは「重量(kg)」
  - グラフ再描画前に必ず前回の `Chart` インスタンスを `destroy()` してから作り直す（重複描画防止）
- 「ホームに戻る」ボタンでホームへ戻る

## 6. データ要件

### 6.1 記録データ（`workout-records`）

localStorageに配列としてJSON保存。1件の構造:

```json
{
  "id": 1723000000000,
  "date": "2026-07-17",
  "part": "胸",
  "exercise": "ベンチプレス",
  "sets": [
    { "weight": 60, "reps": 10 },
    { "weight": 55, "reps": 8 }
  ]
}
```

- `id`: `Date.now()` による生成時刻ベースの一意な数値（削除時の特定に使用）
- `date`: `"YYYY-MM-DD"` 形式の文字列固定。文字列比較のみで期間判定・一致判定が可能になる設計（HANDOVER.mdより）
- `sets`: 「1セット目60kg×10、2セット目55kg×8」のように別々のセットを表現するため、単一数値ではなく配列で保持

### 6.2 種目マスタ（`workout-master`）

localStorageにJSONオブジェクトとして保存。部位名をキー、種目名の配列を値に持つ。初期値は `defaultMaster`（[script.js:38-44](script.js#L38-L44)）:

```json
{
  "胸": ["ベンチプレス", "ダンベルプレス", "チェストプレス", "腕立て伏せ"],
  "背中": ["懸垂", "ラットプルダウン", "デッドリフト", "ローイング"],
  "腹筋": ["クランチ", "プランク", "レッグレイズ"],
  "腕": ["アームカール", "トライセプスプレスダウン", "ダンベルカール"],
  "下半身": ["スクワット", "レッグプレス", "レッグエクステンション", "カーフレイズ"]
}
```

- 「＋ 種目を追加」で入力した種目はこのマスタに追記・永続化される
- 部位は5種固定でHTML側に直接記述されており、部位自体をユーザーが追加する機能はない
- 種目名の重複チェックはない（同名種目を複数回追加できてしまう）

## 7. 非機能要件

- **技術構成**: 素のHTML/CSS/JavaScript（フレームワーク・ビルドツールなし）
- **外部依存**: グラフ描画のみ Chart.js v4 をCDN経由で読み込み（[index.html:124](index.html#L124)）
- **データ永続化**: サーバーなし、`localStorage` のみ
- **公開方法**: GitHub Pages（Publicリポジトリ）
  - リポジトリ: https://github.com/Kaitomatsumoto04/workout-log
  - 公開URL: https://kaitomatsumoto04.github.io/workout-log/
- **開発環境**: VS Code + Live Server（`http://127.0.0.1:5500`）。`file://` 直開きは不可（オリジン制限でJSがエラーになるため）
- **UIデザイン方針**: Apple HIG準拠のダークモード（黒背景ベース）
  - 配色は `:root` のCSS変数（`--blue`, `--red`, `--bg`, `--card`, `--label`, `--secondary`, `--separator`, `--fill`）に集約し一括管理（[style.css:9-18](style.css#L9-L18)）
  - システムフォント（`-apple-system` 等）、17px基準の本文サイズ、角丸12px、枠線の代わりに塗りで区切るなどHIGに準じたスタイル
  - タップ領域はApple推奨の44pt以上を確保（カレンダー送りボタンなど）
- **レスポンシブ対応**: `max-width: 480px` でスマホ幅を想定した中央寄せレイアウト

## 8. 意思決定の背景（採用理由・却下案）

| 項目 | 採用/却下 | 理由 |
|---|---|---|
| フレームワークなし | 採用 | DOM操作・イベント・状態管理の基礎を体で覚えるため。Reactは土台習得後に導入予定 |
| localStorage | 採用 | サーバー不要で即動く。「保存される達成感」を早期に得られる |
| GitHub Pages | 採用 | 無料・push即反映でGit練習と公開が自然につながる |
| Live Server運用 | 採用 | `file://`直開きだとオリジン制限でJSがエラーになるため |
| CSS変数での配色管理 | 採用 | `:root`の値だけでライト/ダーク切替が可能になるため |
| 日付を`"YYYY-MM-DD"`文字列で保持 | 採用 | 文字列比較だけで期間判定・一致判定ができ実装が単純になるため |
| 最初からReact | 却下 | ビルドツールとJS前提知識が必要で、初学段階では挫折リスクが高い |
| 最初からバックエンド+DB | 却下 | 学習コストが高く、最初の1本には重すぎる |
| MVPに月カレンダーを含める | 却下（先送り） | 日付計算・月送り・マス描画だけで単体テーマ級の重さのため。MVP完成後の第3弾に回して実装済み |
| MVPにグラフを含める | 却下（先送り） | 外部ライブラリ（Chart.js）の学習が別途必要なため。第2弾に回して実装済み |
| セット数を1つの数値で保持する設計 | 却下 | 実際のトレーニングは「1セット目60kg×10、2セット目55kg×8」のように別々の値を持つため、`sets`配列に変更 |
| リポジトリのPrivate化 | 却下 | 無料プランではPrivateリポジトリのGitHub Pages公開が不可。記録データ自体は各端末のlocalStorageに閉じるためPublicのまま運用 |
| Claudeによるコード直接編集 | 却下 | 学習効果が落ちるため、以後は提示・行番号案内のみに限定（本人が手を動かす） |

## 9. 未実装・今後の拡張（優先度順）

1. インターバルタイマー（`setInterval` の学習を兼ねる。フロントのみで完結）
2. 前回記録との比較表示
3. PWA化（ホーム画面追加でアプリ風に起動できるようにする）

**第3弾（バックエンド導入・大きなステップアップ）**
- ユーザーアカウント機能＋フレンドの記録閲覧
- 必要になる要素: サーバー＋DB、ログイン認証、フレンド関係の管理
- `localStorage`は端末・ブラウザごとに閉じているため、全端末同期や他人の記録閲覧にはバックエンドが前提となる
- 候補技術: Firebase

**Git運用の次の練習**: ブランチを切って修正→マージ（branch / merge）
